<%= form_for @task, :html => { :class => 'form-horizontal', :id => 'new_task_submit'}, :remote => true do |f| %>
<%= f.hidden_field :goal_id,  :value => @goal.id  unless @goal.nil?%>
<%= f.hidden_field :goal_id,  :value => @task.goal_id  if @goal.nil?%>
  <div class="control-group">
    <%= f.label :name, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :name, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :description, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :description, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :completion_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.date_field :completion_date, :class => 'text_field' %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :start_date, :class => 'control-label' %>
    <div class="controls">
      <%= f.date_field :start_date %>
    </div>
  </div>
  <div class="control-group">
    <%= f.label :money_available, :class => 'control-label' %>
    <div class="controls">
      <%= f.text_field :money_available, :class => 'text_field' %>
    </div>
  </div>


  <div class="form-actions">
    <%= f.submit nil, :class => 'btn btn-primary'%>
  <%= link_to "Cancel", show_tasks_path(:id => @goal.id), :class => 'btn', :remote => true  unless @goal.nil?%>
  <%= link_to "Cancel", show_tasks_path(:id => @task.goal_id), :class => 'btn', :remote => true  if @goal.nil?%>
  </div>
<% end %>
